# OS X temporary files that should never be committed.
.DS_Store

# Xcode private settings:
# window sizes, bookmarks, breakpoints, custom executables and smart groups.
*.pbxuser

# Also, whitelist the default ones, some projects need to use these.
!default.pbxuser

# Throw away ALL personal settings.
xcuserdata/

# Libraries and dependencies should never be committed.
Pods/*
